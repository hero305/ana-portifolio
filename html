<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Ana Beatriz | Portfólio Criativo</title>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&family=Playfair+Display:wght@400;500;600&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        :root {
            --primary-color: #7c4dff;
            --primary-light: #b388ff;
            --secondary-color: #651fff;
            --accent-color: #ff80ab;
            --dark-color: #263238;
            --light-color: #f5f5f7;
            --text-color: #37474f;
            --text-light: #78909c;
            --white: #ffffff;
            --black: #000000;
            
            --font-main: 'Playfair Display', serif;
            --font-secondary: 'Poppins', sans-serif;
            
            --transition: all 0.5s cubic-bezier(0.16, 1, 0.3, 1);
            --shadow: 0 20px 40px rgba(0, 0, 0, 0.08);
            --shadow-hover: 0 30px 60px rgba(0, 0, 0, 0.12);
            --border-radius: 24px;
            --glass-effect: backdrop-filter: blur(16px) saturate(180%);
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        html {
            scroll-behavior: smooth;
            scroll-padding-top: 100px;
        }

        body {
            font-family: var(--font-secondary);
            color: var(--text-color);
            background-color: var(--light-color);
            line-height: 1.8;
            overflow-x: hidden;
        }

        h1, h2, h3, h4, h5, h6 {
            font-family: var(--font-main);
            font-weight: 600;
            line-height: 1.3;
            color: var(--dark-color);
        }

        /* Efeito de gradiente animado suave */
        .gradient-bg {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            z-index: -2;
            background: linear-gradient(-45deg, #f5f7fa, #f1f0ff, #e8e5ff, #e0d9ff);
            background-size: 400% 400%;
            animation: gradient 25s ease infinite;
        }

        @keyframes gradient {
            0% { background-position: 0% 50%; }
            50% { background-position: 100% 50%; }
            100% { background-position: 0% 50%; }
        }

        /* Navbar flutuante elegante */
        .floating-nav {
            position: fixed;
            right: 40px;
            top: 50%;
            transform: translateY(-50%);
            z-index: 1000;
            background-color: rgba(255, 255, 255, 0.96);
            padding: 24px 14px;
            border-radius: 60px;
            box-shadow: var(--shadow);
            backdrop-filter: blur(16px);
            border: 1px solid rgba(255, 255, 255, 0.4);
        }

        .floating-nav ul {
            display: flex;
            flex-direction: column;
            gap: 28px;
        }

        .floating-nav li a {
            width: 52px;
            height: 52px;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            color: var(--text-light);
            font-size: 1.3rem;
            transition: var(--transition);
            position: relative;
            box-shadow: 0 8px 20px rgba(0, 0, 0, 0.06);
        }

        .floating-nav li a::before {
            content: '';
            position: absolute;
            width: 100%;
            height: 100%;
            border-radius: 50%;
            background: linear-gradient(135deg, var(--primary-color), var(--primary-light));
            z-index: -1;
            transform: scale(0);
            transition: var(--transition);
            opacity: 0;
        }

        .floating-nav li a:hover, 
        .floating-nav li a.active {
            color: var(--white);
            transform: translateY(-6px);
            box-shadow: 0 15px 35px rgba(124, 77, 255, 0.25);
        }

        .floating-nav li a:hover::before,
        .floating-nav li a.active::before {
            transform: scale(1);
            opacity: 1;
        }

        /* Seção Hero moderna */
        .hero-section {
            min-height: 100vh;
            display: flex;
            flex-direction: column;
            justify-content: center;
            padding: 140px 0;
            position: relative;
        }

        .hero-content {
            display: flex;
            align-items: center;
            justify-content: space-between;
            gap: 80px;
            max-width: 1400px;
            margin: 0 auto;
            padding: 0 80px;
        }

        .hero-text {
            flex: 1;
            animation: fadeInLeft 1.2s ease;
        }

        .greeting {
            font-size: 1.4rem;
            color: var(--primary-color);
            margin-bottom: 20px;
            font-weight: 600;
            display: inline-block;
            background-color: rgba(124, 77, 255, 0.1);
            padding: 12px 24px;
            border-radius: 50px;
            letter-spacing: 1.5px;
            text-transform: uppercase;
            font-family: var(--font-secondary);
        }

        .name {
            font-size: clamp(3rem, 7vw, 5rem);
            margin-bottom: 24px;
            color: var(--dark-color);
            line-height: 1.2;
            font-weight: 700;
        }

        .name span {
            color: var(--primary-color);
            position: relative;
            display: inline-block;
        }

        .name span::after {
            content: '';
            position: absolute;
            bottom: 10px;
            left: 0;
            width: 100%;
            height: 14px;
            background-color: rgba(124, 77, 255, 0.2);
            z-index: -1;
            border-radius: 10px;
        }

        .title {
            font-size: clamp(1.6rem, 4vw, 2.2rem);
            font-weight: 400;
            color: var(--text-light);
            margin-bottom: 40px;
            max-width: 700px;
            line-height: 1.6;
        }

        .hero-buttons {
            display: flex;
            gap: 28px;
            margin-top: 60px;
            flex-wrap: wrap;
        }

        .btn-main, .btn-secondary {
            padding: 20px 40px;
            border-radius: 50px;
            font-weight: 600;
            font-size: 1.2rem;
            transition: var(--transition);
            display: inline-flex;
            align-items: center;
            gap: 14px;
            position: relative;
            overflow: hidden;
            z-index: 1;
            letter-spacing: 0.8px;
        }

        .btn-main {
            background: linear-gradient(135deg, var(--primary-color), var(--primary-light));
            color: var(--white);
            box-shadow: 0 20px 40px rgba(124, 77, 255, 0.25);
        }

        .btn-main:hover {
            transform: translateY(-6px);
            box-shadow: 0 25px 50px rgba(124, 77, 255, 0.35);
        }

        .btn-secondary {
            background-color: transparent;
            color: var(--primary-color);
            border: 2px solid var(--primary-color);
        }

        .btn-secondary:hover {
            background-color: var(--primary-color);
            color: var(--white);
            transform: translateY(-6px);
            box-shadow: 0 20px 40px rgba(124, 77, 255, 0.2);
        }

        /* Avatar moderno */
        .hero-visual {
            flex: 1;
            display: flex;
            justify-content: center;
            animation: fadeInRight 1.2s ease;
        }

        .avatar-container {
            position: relative;
            width: 450px;
            height: 450px;
        }

        .avatar-circle {
            width: 100%;
            height: 100%;
            border-radius: 50%;
            background: linear-gradient(135deg, rgba(124, 77, 255, 0.1) 0%, rgba(179, 136, 255, 0.1) 100%);
            display: flex;
            align-items: center;
            justify-content: center;
            position: relative;
            overflow: hidden;
            box-shadow: 0 30px 60px rgba(124, 77, 255, 0.15);
        }

        .avatar-initials {
            font-size: 8rem;
            font-weight: 800;
            background: linear-gradient(135deg, var(--primary-color), var(--primary-light));
            -webkit-background-clip: text;
            background-clip: text;
            color: transparent;
            z-index: 2;
        }

        .avatar-circle::before {
            content: '';
            position: absolute;
            width: 150%;
            height: 150%;
            background: conic-gradient(
                transparent 0deg,
                transparent 180deg,
                var(--primary-color) 180deg,
                var(--primary-color) 270deg,
                transparent 270deg,
                transparent 360deg
            );
            animation: rotate 10s linear infinite;
        }

        .avatar-circle::after {
            content: '';
            position: absolute;
            inset: 10px;
            border-radius: 50%;
            background-color: var(--light-color);
        }

        .avatar-decoration {
            position: absolute;
            width: 120%;
            height: 120%;
            top: -10%;
            left: -10%;
            border-radius: 50%;
            border: 24px solid rgba(124, 77, 255, 0.1);
            animation: pulse 4s infinite ease-in-out;
            z-index: -1;
        }

        @keyframes rotate {
            from { transform: rotate(0deg); }
            to { transform: rotate(360deg); }
        }

        /* Indicador de scroll elegante */
        .scroll-indicator {
            position: absolute;
            bottom: 80px;
            left: 50%;
            transform: translateX(-50%);
            display: flex;
            flex-direction: column;
            align-items: center;
            gap: 12px;
        }

        .scroll-indicator span {
            display: block;
            width: 4px;
            height: 30px;
            background: linear-gradient(to bottom, var(--primary-color), var(--primary-light));
            border-radius: 12px;
            animation: bounce 2.5s infinite;
            transform-origin: center;
        }

        .scroll-indicator span:nth-child(1) { animation-delay: 0.1s; }
        .scroll-indicator span:nth-child(2) { animation-delay: 0.2s; height: 24px; }
        .scroll-indicator span:nth-child(3) { animation-delay: 0.3s; height: 18px; }

        .scroll-text {
            font-size: 1rem;
            color: var(--text-light);
            margin-top: 20px;
            opacity: 0.8;
            animation: fadeInOut 2.5s infinite;
            letter-spacing: 1.5px;
            text-transform: uppercase;
            font-family: var(--font-secondary);
            font-weight: 500;
        }

        @keyframes fadeInOut {
            0%, 100% { opacity: 0.6; }
            50% { opacity: 1; }
        }

        @keyframes bounce {
            0%, 100% { transform: translateY(0); }
            50% { transform: translateY(-20px); }
        }

        /* Seções com efeito de vidro refinado */
        section {
            padding: 140px 0;
            position: relative;
        }

        .section-header {
            text-align: center;
            margin-bottom: 100px;
            position: relative;
        }

        .section-header h2 {
            font-size: clamp(2.4rem, 7vw, 3.5rem);
            margin-bottom: 28px;
            display: inline-block;
            position: relative;
        }

        .section-header h2 span {
            color: var(--primary-color);
            position: relative;
        }

        .section-header h2 span::after {
            content: '';
            position: absolute;
            bottom: 10px;
            left: 0;
            width: 100%;
            height: 12px;
            background-color: rgba(124, 77, 255, 0.2);
            z-index: -1;
            border-radius: 10px;
        }

        .section-divider {
            width: 120px;
            height: 6px;
            background: linear-gradient(90deg, var(--primary-color), var(--accent-color));
            margin: 0 auto;
            border-radius: 4px;
            position: relative;
        }

        .section-divider::after {
            content: '';
            position: absolute;
            width: 50px;
            height: 14px;
            background-color: var(--accent-color);
            bottom: -5px;
            left: 50%;
            transform: translateX(-50%);
            border-radius: 8px;
        }

        /* Seção Sobre elegante */
        .about-content {
            display: flex;
            gap: 80px;
            align-items: center;
            max-width: 1400px;
            margin: 0 auto;
            padding: 0 80px;
        }

        .about-text {
            flex: 1;
            background-color: rgba(255, 255, 255, 0.94);
            padding: 60px;
            border-radius: var(--border-radius);
            box-shadow: var(--shadow);
            backdrop-filter: blur(16px);
            border: 1px solid rgba(255, 255, 255, 0.4);
            transition: var(--transition);
        }

        .about-text:hover {
            transform: translateY(-12px);
            box-shadow: var(--shadow-hover);
        }

        .about-text h3 {
            font-size: 2.4rem;
            margin-bottom: 32px;
            color: var(--dark-color);
            position: relative;
            display: inline-block;
        }

        .about-text h3::after {
            content: '';
            position: absolute;
            bottom: 10px;
            left: 0;
            width: 100%;
            height: 12px;
            background-color: rgba(124, 77, 255, 0.2);
            z-index: -1;
            border-radius: 10px;
        }

        .about-text p {
            margin-bottom: 40px;
            color: var(--text-light);
            font-size: 1.3rem;
            line-height: 1.9;
        }

        .personal-info {
            display: grid;
            grid-template-columns: repeat(2, 1fr);
            gap: 32px;
            margin-top: 50px;
        }

        .info-item {
            display: flex;
            gap: 18px;
            align-items: center;
        }

        .info-label {
            font-weight: 600;
            color: var(--dark-color);
            min-width: 140px;
            font-size: 1.2rem;
        }

        .info-value {
            color: var(--text-light);
            background-color: rgba(124, 77, 255, 0.05);
            padding: 12px 24px;
            border-radius: 50px;
            flex: 1;
            font-size: 1.2rem;
        }

        .about-stats {
            flex: 1;
            display: grid;
            gap: 50px;
        }

        .stat-card {
            background-color: rgba(255, 255, 255, 0.94);
            padding: 50px 40px;
            border-radius: var(--border-radius);
            box-shadow: var(--shadow);
            transition: var(--transition);
            text-align: center;
            backdrop-filter: blur(16px);
            border: 1px solid rgba(255, 255, 255, 0.4);
        }

        .stat-card:hover {
            transform: translateY(-15px);
            box-shadow: var(--shadow-hover);
        }

        .stat-icon {
            width: 90px;
            height: 90px;
            background: linear-gradient(135deg, rgba(124, 77, 255, 0.1) 0%, rgba(179, 136, 255, 0.1) 100%);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            margin: 0 auto 32px;
            font-size: 2.4rem;
            color: var(--primary-color);
            border: 1px solid rgba(124, 77, 255, 0.2);
            transition: var(--transition);
        }

        .stat-card:hover .stat-icon {
            transform: rotate(15deg) scale(1.1);
            background: linear-gradient(135deg, var(--primary-color), var(--primary-light));
            color: var(--white);
        }

        .stat-card h3 {
            margin-bottom: 24px;
            font-size: 1.8rem;
            color: var(--dark-color);
        }

        .stat-card p {
            color: var(--text-light);
            font-size: 1.2rem;
            line-height: 1.8;
        }

        /* Seção Projetos elegante */
        .projects-section {
            background-color: rgba(124, 77, 255, 0.03);
        }

        .projects-container {
            max-width: 1400px;
            margin: 0 auto;
            padding: 0 80px;
        }

        .projects-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(420px, 1fr));
            gap: 50px;
        }

        .project-card {
            background-color: rgba(255, 255, 255, 0.94);
            border-radius: var(--border-radius);
            box-shadow: var(--shadow);
            overflow: hidden;
            transition: var(--transition);
            backdrop-filter: blur(16px);
            border: 1px solid rgba(255, 255, 255, 0.4);
        }

        .project-card:hover {
            transform: translateY(-15px);
            box-shadow: var(--shadow-hover);
        }

        .project-image {
            height: 250px;
            background: linear-gradient(135deg, rgba(124, 77, 255, 0.1) 0%, rgba(179, 136, 255, 0.1) 100%);
            display: flex;
            align-items: center;
            justify-content: center;
            color: var(--primary-color);
            font-size: 5rem;
            transition: var(--transition);
        }

        .project-card:hover .project-image {
            background: linear-gradient(135deg, var(--primary-color), var(--primary-light));
            color: var(--white);
        }

        .project-content {
            padding: 40px;
        }

        .project-content h3 {
            margin-bottom: 24px;
            color: var(--primary-color);
            font-size: 1.8rem;
            position: relative;
            display: inline-block;
        }

        .project-content h3::after {
            content: '';
            position: absolute;
            bottom: 8px;
            left: 0;
            width: 100%;
            height: 10px;
            background-color: rgba(124, 77, 255, 0.2);
            z-index: -1;
            border-radius: 8px;
        }

        .project-content p {
            color: var(--text-light);
            margin-bottom: 32px;
            font-size: 1.2rem;
            line-height: 1.8;
        }

        .project-link {
            display: inline-flex;
            align-items: center;
            gap: 12px;
            color: var(--primary-color);
            font-weight: 600;
            font-size: 1.2rem;
            transition: var(--transition);
            padding: 10px 0;
            position: relative;
        }

        .project-link::after {
            content: '';
            position: absolute;
            bottom: 0;
            left: 0;
            width: 0;
            height: 3px;
            background: linear-gradient(90deg, var(--primary-color), var(--accent-color));
            transition: var(--transition);
        }

        .project-link:hover {
            color: var(--secondary-color);
            transform: translateX(10px);
        }

        .project-link:hover::after {
            width: 100%;
        }

        /* Seção Contato elegante */
        .contact-section {
            background-color: rgba(124, 77, 255, 0.03);
        }

        .contact-container {
            display: flex;
            gap: 80px;
            max-width: 1400px;
            margin: 0 auto;
            padding: 0 80px;
        }

        .contact-info {
            flex: 1;
            display: grid;
            grid-template-columns: repeat(2, 1fr);
            gap: 40px;
        }

        .contact-card {
            background-color: rgba(255, 255, 255, 0.94);
            padding: 50px 40px;
            border-radius: var(--border-radius);
            box-shadow: var(--shadow);
            transition: var(--transition);
            text-align: center;
            backdrop-filter: blur(16px);
            border: 1px solid rgba(255, 255, 255, 0.4);
        }

        .contact-card:hover {
            transform: translateY(-12px);
            box-shadow: var(--shadow-hover);
        }

        .contact-icon {
            width: 90px;
            height: 90px;
            background: linear-gradient(135deg, rgba(124, 77, 255, 0.1) 0%, rgba(179, 136, 255, 0.1) 100%);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            margin: 0 auto 32px;
            font-size: 2.4rem;
            color: var(--primary-color);
            border: 1px solid rgba(124, 77, 255, 0.2);
            transition: var(--transition);
        }

        .contact-card:hover .contact-icon {
            transform: scale(1.1);
            background: linear-gradient(135deg, var(--primary-color), var(--primary-light));
            color: var(--white);
        }

        .contact-card h3 {
            margin-bottom: 24px;
            font-size: 1.8rem;
            color: var(--dark-color);
        }

        .contact-card p {
            color: var(--text-light);
            font-size: 1.2rem;
        }

        .contact-form {
            flex: 1;
            background-color: rgba(255, 255, 255, 0.94);
            padding: 60px;
            border-radius: var(--border-radius);
            box-shadow: var(--shadow);
            backdrop-filter: blur(16px);
            border: 1px solid rgba(255, 255, 255, 0.4);
            transition: var(--transition);
        }

        .contact-form:hover {
            transform: translateY(-8px);
            box-shadow: var(--shadow-hover);
        }

        .form-group {
            margin-bottom: 36px;
        }

        .form-group input,
        .form-group textarea {
            width: 100%;
            padding: 20px 30px;
            border: 1px solid rgba(0, 0, 0, 0.1);
            border-radius: var(--border-radius);
            font-family: var(--font-secondary);
            font-size: 1.2rem;
            transition: var(--transition);
            background-color: rgba(255, 255, 255, 0.8);
        }

        .form-group input:focus,
        .form-group textarea:focus {
            outline: none;
            border-color: var(--primary-color);
            box-shadow: 0 0 0 4px rgba(124, 77, 255, 0.2);
            background-color: var(--white);
        }

        .btn-submit {
            background: linear-gradient(135deg, var(--primary-color), var(--primary-light));
            color: var(--white);
            border: none;
            padding: 22px 40px;
            border-radius: 50px;
            font-weight: 600;
            font-size: 1.2rem;
            cursor: pointer;
            transition: var(--transition);
            display: inline-flex;
            align-items: center;
            gap: 14px;
            box-shadow: 0 20px 40px rgba(124, 77, 255, 0.25);
            width: 100%;
            justify-content: center;
            letter-spacing: 0.8px;
        }

        .btn-submit:hover {
            transform: translateY(-6px);
            box-shadow: 0 25px 50px rgba(124, 77, 255, 0.35);
            background: linear-gradient(135deg, var(--secondary-color), var(--primary-color));
        }

        /* Rodapé estiloso */
        .main-footer {
            background-color: var(--dark-color);
            color: var(--white);
            padding: 120px 0 0;
            position: relative;
        }

        .footer-content {
            max-width: 1400px;
            margin: 0 auto;
            padding: 0 80px;
            display: grid;
            grid-template-columns: repeat(3, 1fr);
            gap: 80px;
        }

        .footer-logo {
            display: flex;
            align-items: center;
            gap: 24px;
            margin-bottom: 32px;
        }

        .footer-logo span {
            width: 70px;
            height: 70px;
            background: linear-gradient(135deg, var(--primary-color), var(--primary-light));
            color: var(--white);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 2rem;
            font-weight: 700;
            box-shadow: 0 15px 40px rgba(124, 77, 255, 0.3);
        }

        .footer-logo h3 {
            font-size: 2rem;
            font-weight: 700;
        }

        .footer-about p {
            color: rgba(255, 255, 255, 0.7);
            margin-bottom: 32px;
            line-height: 1.9;
            font-size: 1.2rem;
        }

        .footer-social {
            display: flex;
            gap: 24px;
            margin-top: 32px;
        }

        .footer-social a {
            width: 60px;
            height: 60px;
            border-radius: 50%;
            background-color: rgba(255, 255, 255, 0.1);
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 1.5rem;
            transition: var(--transition);
        }

        .footer-social a:hover {
            background: linear-gradient(135deg, var(--primary-color), var(--primary-light));
            transform: translateY(-6px);
            box-shadow: 0 15px 40px rgba(124, 77, 255, 0.3);
        }

        .footer-links h3 {
            font-size: 1.8rem;
            margin-bottom: 32px;
            position: relative;
            display: inline-block;
        }

        .footer-links h3::after {
            content: '';
            position: absolute;
            bottom: 8px;
            left: 0;
            width: 100%;
            height: 10px;
            background-color: rgba(179, 136, 255, 0.3);
            z-index: -1;
            border-radius: 8px;
        }

        .footer-links ul {
            display: flex;
            flex-direction: column;
            gap: 20px;
        }

        .footer-links a {
            transition: var(--transition);
            display: flex;
            align-items: center;
            gap: 16px;
            color: rgba(255, 255, 255, 0.7);
            font-size: 1.2rem;
        }

        .footer-links a i {
            color: var(--primary-light);
            transition: var(--transition);
        }

        .footer-links a:hover {
            color: var(--white);
            transform: translateX(10px);
        }

        .footer-links a:hover i {
            color: var(--accent-color);
        }

        .footer-bottom {
            text-align: center;
            padding: 40px;
            margin-top: 100px;
            border-top: 1px solid rgba(255, 255, 255, 0.1);
            font-size: 1.1rem;
            color: rgba(255, 255, 255, 0.5);
        }

        /* Animações */
        @keyframes fadeInLeft {
            from {
                opacity: 0;
                transform: translateX(-100px);
            }
            to {
                opacity: 1;
                transform: translateX(0);
            }
        }

        @keyframes fadeInRight {
            from {
                opacity: 0;
                transform: translateX(100px);
            }
            to {
                opacity: 1;
                transform: translateX(0);
            }
        }

        @keyframes pulse {
            0% {
                transform: scale(1);
                opacity: 1;
            }
            50% {
                transform: scale(1.05);
                opacity: 0.8;
            }
            100% {
                transform: scale(1);
                opacity: 1;
            }
        }

        /* Responsividade */
        @media (max-width: 1600px) {
            .hero-content,
            .about-content,
            .contact-container {
                padding: 0 70px;
            }
        }

        @media (max-width: 1400px) {
            .hero-content,
            .about-content,
            .contact-container,
            .projects-container {
                padding: 0 60px;
            }
            
            .footer-content {
                padding: 0 60px;
            }
        }

        @media (max-width: 1200px) {
            .hero-content,
            .about-content,
            .contact-container,
            .projects-container {
                padding: 0 50px;
            }
            
            .footer-content {
                padding: 0 50px;
            }
            
            .avatar-container {
                width: 400px;
                height: 400px;
            }
        }

        @media (max-width: 992px) {
            .hero-content {
                flex-direction: column;
                text-align: center;
                gap: 100px;
            }
            
            .hero-buttons {
                justify-content: center;
            }
            
            .about-content {
                flex-direction: column;
                gap: 80px;
            }
            
            .contact-container {
                flex-direction: column;
                gap: 80px;
            }
            
            .footer-content {
                grid-template-columns: 1fr 1fr;
                gap: 70px;
            }
            
            .avatar-container {
                width: 380px;
                height: 380px;
            }
            
            .floating-nav {
                right: 30px;
                padding: 20px 12px;
            }
            
            .floating-nav li a {
                width: 50px;
                height: 50px;
                font-size: 1.2rem;
            }
        }

        @media (max-width: 768px) {
            .hero-content,
            .about-content,
            .contact-container,
            .projects-container {
                padding: 0 40px;
            }
            
            .name {
                font-size: 3.5rem;
            }
            
            .title {
                font-size: 1.8rem;
            }
            
            .personal-info {
                grid-template-columns: 1fr;
            }
            
            .projects-grid {
                grid-template-columns: 1fr;
            }
            
            .footer-content {
                grid-template-columns: 1fr;
                gap: 60px;
            }
            
            .avatar-container {
                width: 340px;
                height: 340px;
            }
            
            .floating-nav {
                right: 25px;
                padding: 18px 10px;
            }
        }

        @media (max-width: 576px) {
            .hero-content,
            .about-content,
            .contact-container,
            .projects-container {
                padding: 0 30px;
            }
            
            .name {
                font-size: 3rem;
            }
            
            .title {
                font-size: 1.6rem;
            }
            
            .hero-buttons {
                flex-direction: column;
                gap: 24px;
            }
            
            .btn-main, .btn-secondary {
                width: 100%;
                justify-content: center;
            }
            
            .avatar-container {
                width: 300px;
                height: 300px;
            }
            
            .floating-nav {
                bottom: 30px;
                top: auto;
                right: 50%;
                transform: translateX(50%);
                flex-direction: row;
                padding: 16px 24px;
            }
            
            .floating-nav ul {
                flex-direction: row;
                gap: 24px;
            }
            
            .contact-info {
                grid-template-columns: 1fr;
            }
            
            .section-header h2 {
                font-size: 2.6rem;
            }
        }
    </style>
</head>
<body>
    <div class="gradient-bg"></div>
    
    <nav class="floating-nav">
        <ul>
            <li><a href="#home" class="nav-link active"><i class="fas fa-home"></i></a></li>
            <li><a href="#about" class="nav-link"><i class="fas fa-user"></i></a></li>
            <li><a href="#projects" class="nav-link"><i class="fas fa-project-diagram"></i></a></li>
            <li><a href="#contact" class="nav-link"><i class="fas fa-envelope"></i></a></li>
        </ul>
    </nav>

    <main>
        <section id="home" class="hero-section">
            <div class="hero-content">
                <div class="hero-text">
                    <h4 class="greeting">Olá, eu sou</h4>
                    <h1 class="name">Ana <span>Beatriz</span></h1>
                    <h2 class="title">Estudante e Entusiasta de Programação</h2>
                    <div class="hero-buttons">
                        <a href="#projects" class="btn-main"><i class="fas fa-project-diagram"></i> Meus Projetos</a>
                        <a href="#about" class="btn-secondary"><i class="fas fa-user"></i> Sobre Mim</a>
                    </div>
                </div>
                <div class="hero-visual">
                    <div class="avatar-container">
                        <div class="avatar-circle">
                            <div class="avatar-initials">AB</div>
                        </div>
                        <div class="avatar-decoration"></div>
                    </div>
                </div>
            </div>
           
